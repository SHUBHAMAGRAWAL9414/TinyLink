PORT=4000
# BASE_URL=http://localhost:4000
BASE_URL=https://tiny-link-omega.vercel.app
NODE_ENV=production
// NOTE: This file is a template for environment variables used by the backend.
// Remove the comments and fill values into a local `.env` when running locally.
# MongoDB connection string (replace with your MongoDB Atlas or self-hosted URI)
# Example: MONGODB_URI=mongodb+srv://user:pass@cluster0.mongodb.net/tinylink?retryWrites=true&w=majority
MONGODB_URI=

# Optional: comma-separated list of allowed origins for CORS.
# When set, this value is used instead of BASE_URL for CORS origin checks.
# Example: ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=
